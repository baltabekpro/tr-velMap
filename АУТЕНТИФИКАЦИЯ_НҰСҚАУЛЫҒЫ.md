# 🔐 АУТЕНТИФИКАЦИЯ ЖҮЙЕСІ - ТОЛЫҚ НҰСҚАУЛЫҚ

## 📋 Мазмұны

1. [Жалпы ақпарат](#жалпы-ақпарат)
2. [Архитектура](#архитектура)
3. [Дерекқор құрылымы](#дерекқор-құрылымы)
4. [API эндпоинттері](#api-эндпоинттері)
5. [Frontend беттер](#frontend-беттер)
6. [Қауіпсіздік](#қауіпсіздік)
7. [Пайдалану мысалдары](#пайдалану-мысалдары)

---

## 🎯 Жалпы ақпарат

Auth Service - бұл **порт 3005** үстінде жұмыс істейтін микросервис, пайдаланушы аутентификациясы, авторизациясы және профиль басқару үшін жауапты.

### Негізгі мүмкіндіктер

- ✅ **Тіркелу (Registration)** - жаңа пайдаланушы құру
- ✅ **Кіру (Login)** - JWT токен алу
- ✅ **Профиль басқару** - деректерді көру/өңдеу
- ✅ **Құпия сөз өзгерту** - қауіпсіздік
- ✅ **Құпиялық баптаулары** - деректер көрінісін бақылау
- ✅ **Әкімші панелі** - пайдаланушыларды басқару
- ✅ **Рөлдер жүйесі** - қол жетімділікті бақылау

### Технологиялар

| Технология | Не үшін | Версия |
|-----------|---------|--------|
| **JWT** | Токен-негізделген аутентификация | 9.0.2 |
| **bcryptjs** | Құпия сөзді хэштеу | 2.4.3 |
| **SQLite3** | Дерекқор | 5.1.6 |
| **Express.js** | REST API | 4.18.2 |

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────┐
│              FRONTEND (Browser)                  │
│  login.html | profile.html | admin.html         │
│  localStorage (JWT Token)                        │
└─────────────────────────────────────────────────┘
                      │
                      ▼ HTTP Requests
┌─────────────────────────────────────────────────┐
│           API GATEWAY (:3000)                    │
│  CORS | Proxy | Health Check                    │
└─────────────────────────────────────────────────┘
                      │
                      ▼ /api/auth/*
┌─────────────────────────────────────────────────┐
│           AUTH SERVICE (:3005)                   │
│  JWT Middleware | bcrypt | RBAC                 │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│         SQLite3 Database (travelmap.db)          │
│  users | sessions | reviews | stats | logs      │
└─────────────────────────────────────────────────┘
```

---

## 🗄️ Дерекқор құрылымы

### 1. users - Пайдаланушылар

| Бағана | Түрі | Сипаттама |
|--------|------|-----------|
| id | INTEGER PRIMARY KEY | Уникалды ID |
| username | TEXT UNIQUE | Пайдаланушы аты |
| email | TEXT UNIQUE | Email мекенжайы |
| password_hash | TEXT | Хэштелген құпия сөз |
| full_name | TEXT | Толық аты |
| avatar_url | TEXT | Аватар суреті |
| role | TEXT | user/moderator/admin |
| status | TEXT | active/banned/pending |
| phone | TEXT | Телефон |
| birth_date | TEXT | Туған күні |
| gender | TEXT | Жынысы |
| bio | TEXT | Өзі туралы |
| country | TEXT | Ел |
| city | TEXT | Қала |
| language | TEXT | Тіл (kk/ru/en) |
| created_at | TEXT | Тіркелген уақыты |
| last_login | TEXT | Соңғы кіру уақыты |
| email_verified | INTEGER | Email расталған ба |
| phone_verified | INTEGER | Телефон расталған ба |

### 2. user_sessions - Сессиялар

| Бағана | Түрі | Сипаттама |
|--------|------|-----------|
| id | INTEGER PRIMARY KEY | Сессия ID |
| user_id | INTEGER | Пайдаланушы ID |
| token | TEXT UNIQUE | JWT токен |
| expires_at | TEXT | Аяқталу уақыты |
| created_at | TEXT | Құрылған уақыты |
| ip_address | TEXT | IP мекенжайы |
| user_agent | TEXT | Браузер ақпараты |

### 3. user_favorites - Таңдаулы орындар

```sql
id, user_id, place_id, added_at, notes
```

### 4. user_visits - Бару орындары тарихы

```sql
id, user_id, place_id, visit_date, rating, review, photos
```

### 5. user_reviews - Пікірлер

```sql
id, user_id, place_id, rating, title, content, likes, dislikes, 
status (pending/approved/rejected), created_at, updated_at
```

### 6. user_achievements - Жетістіктер

```sql
id, user_id, achievement_type, achievement_name, description, 
icon, earned_at
```

### 7. user_stats - Статистика

```sql
user_id, total_visits, total_reviews, total_favorites, 
total_achievements, profile_views, last_active
```

### 8. user_privacy_settings - Құпиялық

```sql
user_id, profile_public, show_email, show_phone, show_visits, 
show_reviews, show_favorites, allow_messages
```

### 9. admin_logs - Әкімші журналы

```sql
id, admin_id, admin_username, action (ban_user, delete_review, etc.),
target_user_id, target_username, details, created_at
```

### 10. notifications - Хабарламалар

```sql
id, user_id, type, title, message, link, is_read, created_at
```

---

## 🔌 API эндпоинттері

### Ашық эндпоинттер (токенсіз)

#### 1. Тіркелу
```http
POST /api/auth/register
Content-Type: application/json

{
  "username": "john_doe",
  "email": "john@example.com",
  "password": "securepass123",
  "full_name": "John Doe"
}
```

**Жауап (201 Created):**
```json
{
  "message": "Пайдаланушы сәтті тіркелді",
  "userId": 5
}
```

**Қателер:**
- 400 - Валидация қатесі
- 409 - Username/Email бос емес

#### 2. Кіру
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123"
}
```

**Жауап (200 OK):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "admin",
    "email": "admin@travelmap.kz",
    "full_name": "Администратор",
    "role": "admin",
    "status": "active"
  }
}
```

**Қателер:**
- 400 - Username/password жоқ
- 401 - Қате username/password
- 403 - Пайдаланушы бұғатталған

#### 3. Токенді тексеру
```http
POST /api/auth/verify
Content-Type: application/json

{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**Жауап (200 OK):**
```json
{
  "valid": true,
  "user": {
    "id": 1,
    "username": "admin",
    "email": "admin@travelmap.kz",
    "role": "admin"
  }
}
```

### Қорғалған эндпоинттер (JWT токен қажет)

**Барлық сұраулар үшін қажет:**
```http
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### 4. Профильді көру
```http
GET /api/auth/profile
Authorization: Bearer {token}
```

**Жауап (200 OK):**
```json
{
  "user": {
    "id": 1,
    "username": "admin",
    "email": "admin@travelmap.kz",
    "full_name": "Администратор",
    "role": "admin",
    "status": "active",
    "phone": "+7 777 123 4567",
    "birth_date": "1990-01-01",
    "gender": "male",
    "bio": "Travel enthusiast",
    "country": "Қазақстан",
    "city": "Алматы",
    "language": "kk",
    "created_at": "2024-01-01T00:00:00.000Z"
  },
  "stats": {
    "total_visits": 15,
    "total_reviews": 8,
    "total_favorites": 12,
    "total_achievements": 5
  },
  "privacy": {
    "profile_public": 1,
    "show_email": 0,
    "show_phone": 0,
    "show_visits": 1,
    "show_reviews": 1,
    "show_favorites": 1,
    "allow_messages": 1
  }
}
```

#### 5. Профильді жаңарту
```http
PUT /api/auth/profile
Authorization: Bearer {token}
Content-Type: application/json

{
  "full_name": "John Smith",
  "phone": "+7 777 999 8888",
  "bio": "Love traveling!",
  "city": "Астана"
}
```

**Жауап (200 OK):**
```json
{
  "message": "Профиль сәтті жаңартылды"
}
```

#### 6. Құпия сөзді өзгерту
```http
PUT /api/auth/change-password
Authorization: Bearer {token}
Content-Type: application/json

{
  "old_password": "oldpass123",
  "new_password": "newpass456"
}
```

**Жауап (200 OK):**
```json
{
  "message": "Құпия сөз сәтті өзгертілді"
}
```

**Қателер:**
- 400 - Ескі құпия сөз қате
- 400 - Жаңа құпия сөз өте қысқа

#### 7. Құпиялық баптаулары
```http
PUT /api/auth/privacy-settings
Authorization: Bearer {token}
Content-Type: application/json

{
  "profile_public": 1,
  "show_email": 0,
  "show_phone": 0,
  "show_visits": 1,
  "show_reviews": 1,
  "show_favorites": 1,
  "allow_messages": 1
}
```

**Жауап (200 OK):**
```json
{
  "message": "Құпиялық баптаулары сақталды"
}
```

### Әкімші эндпоинттері (admin рөлі қажет)

#### 8. Пайдаланушылар тізімі
```http
GET /api/auth/admin/users?page=1&limit=20&role=user&status=active&search=john
Authorization: Bearer {admin_token}
```

**Query параметрлер:**
- `page` - Бет нөмірі (әдепкі: 1)
- `limit` - Беттегі жазбалар саны (әдепкі: 20)
- `role` - Рөл сүзгісі (user/moderator/admin)
- `status` - Статус сүзгісі (active/banned/pending)
- `search` - Іздеу (username, email, full_name)

**Жауап (200 OK):**
```json
{
  "users": [
    {
      "id": 5,
      "username": "john_doe",
      "email": "john@example.com",
      "full_name": "John Doe",
      "role": "user",
      "status": "active",
      "created_at": "2024-06-01T10:00:00.000Z",
      "last_login": "2024-11-05T15:30:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 45,
    "pages": 3
  }
}
```

#### 9. Рөлді өзгерту
```http
PUT /api/auth/admin/users/5/role
Authorization: Bearer {admin_token}
Content-Type: application/json

{
  "role": "moderator"
}
```

**Жауап (200 OK):**
```json
{
  "message": "Пайдаланушы рөлі өзгертілді"
}
```

#### 10. Пайдаланушыны бұғаттау/разбан
```http
PUT /api/auth/admin/users/5/status
Authorization: Bearer {admin_token}
Content-Type: application/json

{
  "status": "banned"
}
```

**Жауап (200 OK):**
```json
{
  "message": "Пайдаланушы статусы өзгертілді"
}
```

#### 11. Пайдаланушыны жою
```http
DELETE /api/auth/admin/users/5
Authorization: Bearer {admin_token}
```

**Жауап (200 OK):**
```json
{
  "message": "Пайдаланушы жойылды"
}
```

**Ескерту:** Өзін өзі жою мүмкін емес!

#### 12. Жүйе статистикасы
```http
GET /api/auth/admin/stats
Authorization: Bearer {admin_token}
```

**Жауап (200 OK):**
```json
{
  "totalUsers": 156,
  "activeUsers": 142,
  "bannedUsers": 8,
  "admins": 3,
  "totalReviews": 487,
  "totalVisits": 1234,
  "recentUsers": [
    {
      "id": 156,
      "username": "newuser",
      "created_at": "2024-11-06T08:00:00.000Z"
    }
  ]
}
```

#### 13. Әкімші журналы
```http
GET /api/auth/admin/logs?page=1&limit=50
Authorization: Bearer {admin_token}
```

**Жауап (200 OK):**
```json
{
  "logs": [
    {
      "id": 123,
      "admin_username": "admin",
      "action": "ban_user",
      "target_username": "bad_user",
      "details": "Спам үшін бұғатталды",
      "created_at": "2024-11-06T10:30:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 50,
    "total": 234
  }
}
```

---

## 🖥️ Frontend беттер

### 1. login.html - Кіру/Тіркелу

**Орналасуы:** `/frontend/login.html`

**Мүмкіндіктер:**
- Екі форма: Кіру және Тіркелу
- Tab-пен ауыстыру
- Құпия сөз күшін тексеру
- Валидация
- Автоматты редирект:
  - Admin → `admin.html`
  - User → `profile.html`

**Өрістер (Тіркелу):**
- Username (минимум 3 таңба)
- Email (валидация)
- Full name (опционал)
- Password (минимум 6 таңба)
- Confirm password

**Өрістер (Кіру):**
- Username немесе Email
- Password

### 2. profile.html - Профиль

**Орналасуы:** `/frontend/profile.html`

**Бөлімдер:**
1. **Профиль** - жеке деректерді өңдеу
2. **Құпия сөз** - құпия сөзді өзгерту
3. **Құпиялық** - құпиялық баптаулары

**Статистика:**
- Бару орындары саны
- Пікірлер саны
- Таңдаулылар саны
- Жетістіктер саны

**Өрістер:**
- Full name
- Email (тек оқу үшін)
- Phone
- Birth date
- Gender
- Language
- Country
- City
- Bio (textarea)

### 3. admin.html - Әкімші панелі

**Орналасуы:** `/frontend/admin.html`

**Тек admin рөлі үшін қолжетімді!**

**Статистика карталары:**
- Барлық пайдаланушылар
- Белсенді пайдаланушылар
- Бұғатталған пайдаланушылар
- Әкімшілер саны
- Пікірлер саны
- Бару орындары саны

**Пайдаланушылар тізімі:**
- Іздеу (username, email, full name)
- Сүзгілер:
  - Рөл (user/moderator/admin)
  - Статус (active/banned/pending)
- Әрекеттер:
  - Рөлді өзгерту (user → moderator → admin → user)
  - Бұғаттау/Разбан
  - Жою (растау керек)

**Әкімші журналы:**
- Барлық әкімші әрекеттері
- Уақыт белгілері
- Әрекет түрлері
- Мақсат пайдаланушы

---

## 🔒 Қауіпсіздік

### 1. JWT Токендер

**Конфигурация:**
```javascript
JWT_SECRET = process.env.JWT_SECRET || 'travelmap-secret-key-2024'
JWT_EXPIRES_IN = '7d'
```

**Токен құрылымы:**
```json
{
  "userId": 1,
  "username": "admin",
  "email": "admin@travelmap.kz",
  "role": "admin",
  "iat": 1699264800,
  "exp": 1699869600
}
```

### 2. Құпия сөз хэштеу

**bcrypt параметрлері:**
```javascript
const saltRounds = 10;
const hash = await bcrypt.hash(password, saltRounds);
```

**Тексеру:**
```javascript
const isValid = await bcrypt.compare(password, hash);
```

### 3. Валидация

**Username:**
- Минимум 3 таңба
- Уникалды болуы керек

**Email:**
- Email формат тексеру
- Уникалды болуы керек

**Password:**
- Минимум 6 таңба
- Ұсынылады: әріптер + сандар + символдар

### 4. Role-Based Access Control (RBAC)

**Рөлдер:**
1. **user** - қарапайым пайдаланушы
2. **moderator** - модератор
3. **admin** - әкімші

**Middleware:**
```javascript
function requireAdmin(req, res, next) {
    if (req.user.role !== 'admin') {
        return res.status(403).json({ 
            error: 'Қол жетімділік жоқ' 
        });
    }
    next();
}
```

### 5. SQL Injection қорғанысы

**Parameterized queries қолдану:**
```javascript
// ✅ ДҰРЫС
db.run(
    'INSERT INTO users (username, email) VALUES (?, ?)',
    [username, email]
);

// ❌ ҚАТЕ
db.run(
    `INSERT INTO users (username, email) VALUES ('${username}', '${email}')`
);
```

### 6. Әкімші қорғанысы

- ✅ Өзін өзі жою мүмкін емес
- ✅ Барлық әрекеттер журналға жазылады
- ✅ Тек admin рөлі қол жеткізе алады

---

## 💡 Пайдалану мысалдары

### Frontend-тен пайдалану

#### 1. Тіркелу
```javascript
async function register() {
    const response = await fetch('http://localhost:3000/api/auth/register', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
            username: 'john_doe',
            email: 'john@example.com',
            password: 'securepass123',
            full_name: 'John Doe'
        })
    });
    
    const data = await response.json();
    console.log(data);
}
```

#### 2. Кіру және токенді сақтау
```javascript
async function login() {
    const response = await fetch('http://localhost:3000/api/auth/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
            username: 'admin',
            password: 'admin123'
        })
    });
    
    const data = await response.json();
    
    if (data.token) {
        // Токенді localStorage-ке сақтау
        localStorage.setItem('token', data.token);
        localStorage.setItem('user', JSON.stringify(data.user));
        
        // Редирект
        if (data.user.role === 'admin') {
            window.location.href = 'admin.html';
        } else {
            window.location.href = 'profile.html';
        }
    }
}
```

#### 3. Профильді көру
```javascript
async function getProfile() {
    const token = localStorage.getItem('token');
    
    const response = await fetch('http://localhost:3000/api/auth/profile', {
        headers: {
            'Authorization': `Bearer ${token}`
        }
    });
    
    const data = await response.json();
    console.log(data);
}
```

#### 4. Профильді жаңарту
```javascript
async function updateProfile() {
    const token = localStorage.getItem('token');
    
    const response = await fetch('http://localhost:3000/api/auth/profile', {
        method: 'PUT',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${token}`
        },
        body: JSON.stringify({
            full_name: 'John Smith',
            city: 'Астана',
            bio: 'Love traveling!'
        })
    });
    
    const data = await response.json();
    console.log(data);
}
```

#### 5. Шығу
```javascript
function logout() {
    localStorage.removeItem('token');
    localStorage.removeItem('user');
    window.location.href = 'login.html';
}
```

#### 6. Токенді тексеру (бет жүктелгенде)
```javascript
window.addEventListener('load', async () => {
    const token = localStorage.getItem('token');
    
    if (!token) {
        window.location.href = 'login.html';
        return;
    }
    
    // Токенді тексеру
    const response = await fetch('http://localhost:3000/api/auth/verify', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ token })
    });
    
    const data = await response.json();
    
    if (!data.valid) {
        localStorage.removeItem('token');
        localStorage.removeItem('user');
        window.location.href = 'login.html';
    }
});
```

### cURL мысалдары

#### Тіркелу
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "email": "john@example.com",
    "password": "securepass123",
    "full_name": "John Doe"
  }'
```

#### Кіру
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

#### Профильді көру
```bash
TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

curl http://localhost:3000/api/auth/profile \
  -H "Authorization: Bearer $TOKEN"
```

#### Әкімші: пайдаланушыларды көру
```bash
curl "http://localhost:3000/api/auth/admin/users?page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"
```

#### Әкімші: пайдаланушыны бұғаттау
```bash
curl -X PUT http://localhost:3000/api/auth/admin/users/5/status \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status": "banned"}'
```

---

## 🚀 Іске қосу

### 1. Дерекқорды инициализациялау
```bash
cd backend
npm run init:db
```

### 2. Auth Service іске қосу
```bash
npm run start:auth
```

### 3. Барлық сервистерді іске қосу
```bash
npm run start:all
# немесе
npm run dev:all
```

### 4. Тестілеу

**Health check:**
```bash
curl http://localhost:3005/health
```

**Әдепкі админмен кіру:**
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

---

## 📝 Әдепкі деректер

### Әкімші аккаунты

```
Username: admin
Email:    admin@travelmap.kz
Password: admin123
Role:     admin
Status:   active
```

⚠️ **МАҢЫЗДЫ:** Өндіріске шығарғанда құпия сөзді өзгертіңіз!

```bash
# Әкімшіге кірген соң құпия сөзді өзгерту
curl -X PUT http://localhost:3000/api/auth/change-password \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "old_password": "admin123",
    "new_password": "YourSecurePassword123!"
  }'
```

---

## 🐛 Мәселелерді шешу

### Қате: "Database файлы табылмады"

**Шешім:**
```bash
cd backend
npm run init:db
```

### Қате: "Токен жарамсыз"

**Себептері:**
- Токен мерзімі өтіп кеткен (7 күннен көп)
- JWT_SECRET өзгерген
- Токен бұзылған

**Шешім:**
- Қайта кіру
- Жаңа токен алу

### Қате: "Қол жетімділік жоқ"

**Себептері:**
- User рөлімен admin эндпоинтіне кіру
- Токенсіз қорғалған эндпоинтке кіру

**Шешім:**
- Рөлді тексеру
- Токенді қосу

---

## 📚 Қосымша ақпарат

- **Микросервистер нұсқаулығы:** `МИКРОСЕРВИСТЕР_НҰСҚАУЛЫҒЫ.md`
- **Жалпы README:** `README.md`
- **Жоба құрылымы:** `ҚҰРЫЛЫМ_ТҮСІНДІРМЕСІ.md`

---

**Сәттілік тілейміз! 🔐✨**
