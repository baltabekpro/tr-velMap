# 🚀 МИКРОСЕРВИСТЕРДІ ІСКЕ ҚОСУ НҰСҚАУЛЫҒЫ

## 📋 Жалпы Ақпарат

TravelMap жобасы **микросервистік архитектураға** негізделген. Әр сервис өз бетінше жұмыс істейді және өзінің портында іске қосылады.

### 🔧 Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    API GATEWAY (:3000)                   │
│              Барлық сұраныстарды маршруттау              │
└─────────────────────────────────────────────────────────┘
                             │
            ┌────────────────┼────────────────┬────────────┐
            │                │                │            │
            ▼                ▼                ▼            ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │   Places     │ │   Weather    │ │     Map      │ │     Auth     │
    │   :3001      │ │   :3002      │ │   :3003      │ │   :3005      │
    └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
                             │
                             ▼
                      ┌──────────────┐
                      │     Chat     │
                      │   :3004      │
                      └──────────────┘
```

### 📍 Порттар

| Сервис | Порт | Мақсаты |
|--------|------|---------|
| **API Gateway** | 3000 | Барлық сұраныстарды басқару |
| **Places Service** | 3001 | Орындар деректері (CRUD) |
| **Weather Service** | 3002 | Ауа райы мәліметтері (Open-Meteo) |
| **Map Service** | 3003 | Карта маркерлері және шекаралар |
| **Chat Service** | 3004 | Чатбот AI жауаптары |
| **Auth Service** | 3005 | Аутентификация және авторизация |

---

## 🎯 ІСКЕ ҚОСУ ТӘСІЛДЕРІ

### 1️⃣ БАРЛЫҚ СЕРВИСТЕРДІ БІРГЕ ІСКЕ ҚОСУ (Ұсынылады)

```bash
cd backend
npm run start:all
```

**Не болады:**
- ✅ API Gateway (порт 3000) іске қосылады
- ✅ 4 микросервис паралелльді түрде іске қосылады
- ✅ Барлық сервистер өз портында жұмыс істейді
- ✅ Gateway автоматты түрде сұраныстарды маршруттайды

**Консольдегі шығыс:**
```
✅ [GATEWAY] Жұмыс істеп тұр: http://localhost:3000
✅ [PLACES SERVICE] Жұмыс істеп тұр: http://localhost:3001
✅ [WEATHER SERVICE] Жұмыс істеп тұр: http://localhost:3002
✅ [MAP SERVICE] Жұмыс істеп тұр: http://localhost:3003
✅ [CHAT SERVICE] Жұмыс істеп тұр: http://localhost:3004
```

---

### 2️⃣ ЖЕКЕ СЕРВИСТЕРДІ ІСКЕ ҚОСУ

#### API Gateway (міндетті!)
```bash
cd backend
npm run start:gateway
```

#### Орындар Сервисі
```bash
cd backend
npm run start:places
```

#### Ауа Райы Сервисі
```bash
cd backend
npm run start:weather
```

#### Карта Сервисі
```bash
cd backend
npm run start:map
```

#### Чатбот Сервисі
```bash
cd backend
npm run start:chat
```

#### Аутентификация Сервисі
```bash
cd backend
npm run start:auth
```

---

### 3️⃣ DEVELOPMENT РЕЖИМІ (Auto-restart)

Барлық сервистерді nodemon арқылы іске қосу:

```bash
cd backend
npm run dev:all
```

**Артықшылықтары:**
- 🔄 Файлдар өзгерген кезде автоматты қайта іске қосылады
- 🛠️ Development үшін ыңғайлы
- 📝 Код өзгерістерін тез тестілеуге болады

---

## 🌐 FRONTEND-ТІ ІСКЕ ҚОСУ

### Вариант 1: Live Server (VS Code)
1. VS Code-те `frontend/index.html` файлын ашыңыз
2. Right click → **Open with Live Server**
3. Браузерде `http://localhost:5500/frontend/index.html` ашылады

### Вариант 2: Тікелей Браузерде
1. `frontend/index.html` файлын екі рет басыңыз
2. Браузер автоматты ашылады

### Вариант 3: HTTP Server (Node.js)
```bash
npx http-server frontend -p 8080
```
Содан кейін: `http://localhost:8080`

---

## 📊 МОНИТОРИНГ

### Мониторинг Интерфейсін Ашу
```
http://localhost:5500/frontend/monitor.html
```

**Көрсетілетін ақпарат:**
- ✅ Әр сервистің статусы (online/offline)
- 📊 Uptime уақыты
- 🔢 Порт нөмірлері
- 🕐 Соңғы жаңарту уақыты
- 🔄 Автоматты жаңарту (5 сек)

---

## 🔍 HEALTH CHECK ЭНДПОИНТТЕРІ

### API Gateway Health Check
```bash
curl http://localhost:3000/api/health
```

**Жауап:**
```json
{
  "gateway": {
    "status": "online",
    "port": 3000,
    "timestamp": "2025-11-06T12:00:00.000Z",
    "uptime": 123.45
  },
  "services": {
    "places": { "status": "online", "port": 3001 },
    "weather": { "status": "online", "port": 3002 },
    "map": { "status": "online", "port": 3003 },
    "chat": { "status": "online", "port": 3004 },
    "auth": { "status": "online", "port": 3005 }
  },
  "overall": "healthy"
}
```

### Жеке Сервис Health Check
```bash
curl http://localhost:3001/health  # Places
curl http://localhost:3002/health  # Weather
curl http://localhost:3003/health  # Map
curl http://localhost:3004/health  # Chat
curl http://localhost:3005/health  # Auth
```

---

## 🛠️ МӘСЕЛЕЛЕРДІ ШЕШУ

### Қате: "EADDRINUSE" (Порт бос емес)

**Себебі:** Порт басқа процесс қолданып тұр.

**Шешім:**
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <process_id> /F

# Linux/Mac
lsof -i :3000
kill -9 <process_id>
```

### Қате: "Cannot GET /api/..."

**Себебі:** API Gateway іске қосылмаған.

**Шешім:**
```bash
cd backend
npm run start:gateway
```

### Қате: "Service temporarily unavailable"

**Себебі:** Микросервис жұмыс істемейді.

**Шешім:**
1. Мониторинг парақшасын ашыңыз: `monitor.html`
2. Қай сервис offline екенін көріңіз
3. Сол сервисті іске қосыңыз:
```bash
npm run start:places   # немесе басқа сервис
```

---

## 📦 ТОЛЫҚ ҚАЙТА ІСКЕ ҚОСУ

Егер бір процесс өшіп қалса немесе қайта іске қосу керек болса:

```bash
# 1. Барлық Node процесстарын тоқтату
# Windows:
taskkill /F /IM node.exe

# Linux/Mac:
killall node

# 2. Барлық сервистерді қайта іске қосу
cd backend
npm run start:all
```

---

## 🎓 ҚЫСҚАША НҰСҚАУЛЫҚ

### Бастапқы іске қосу (бірінші рет)
```bash
# 1. Қажетті пакеттерді орнату
cd backend
npm install

# 2. Дерекқорды инициализациялау
npm run init:db

# 3. Барлық сервистерді іске қосу
npm run start:all

# 4. Frontend ашу
# VS Code: Open with Live Server
# Немесе: firefox frontend/index.html
```

### Күнделікті дамыту (development)
```bash
cd backend
npm run dev:all
```

### Өндірісте қолдану (production)
```bash
cd backend
npm run start:all
```

---

## 📞 ҚОСЫМША АҚПАРАТ

- 📄 Толық API құжаттамасы: `backend/API_ҚҰЖАТТАМА.md`
- 📊 Жоба құрылымы: `ҚҰРЫЛЫМ_ТҮСІНДІРМЕСІ.md`
- 🎤 Презентация нұсқаулығы: `ПРЕЗЕНТАЦИЯ_НҰСҚАУЛЫҒЫ.md`

---

## ✅ ТЕКСЕРУ ТІЗІМІ

Барлығы дұрыс жұмыс істеп тұрғанын тексеру үшін:

- [ ] API Gateway (3000) іске қосылды
- [ ] Places Service (3001) іске қосылды  
- [ ] Weather Service (3002) іске қосылды
- [ ] Map Service (3003) іске қосылды
- [ ] Chat Service (3004) іске қосылды
- [ ] Auth Service (3005) іске қосылды
- [ ] Database (SQLite3) инициализацияланды
- [ ] Frontend қолжетімді
- [ ] Мониторинг парақшасы жұмыс істейді
- [ ] Барлық API эндпоинттері жауап береді
- [ ] Логин/Тіркелу беттері жұмыс істейді
- [ ] Админ панелі қолжетімді (admin/admin123)

**Барлығы жасыл болса - жоба дайын! 🎉**

---

## 🔐 АУТЕНТИФИКАЦИЯ ЖҮЙЕСІ

### Auth Service (Порт 3005)

Auth Service - бұл пайдаланушы аутентификациясы, авторизациясы және профиль басқару үшін микросервис.

#### Технологиялар
- **JWT (JSON Web Tokens)** - токен-негізделген аутентификация
- **bcryptjs** - құпия сөзді хэштеу
- **SQLite3** - пайдаланушы деректерін сақтау

#### Дерекқор Құрылымы

```sql
- users - пайдаланушылар
- user_sessions - JWT токендер
- user_favorites - таңдаулы орындар
- user_visits - бару орындары тарихы
- user_reviews - пайдаланушы пікірлері
- user_achievements - жетістіктер жүйесі
- user_stats - статистика
- user_privacy_settings - құпиялық баптаулары
- admin_logs - әкімші әрекеттері журналы
- notifications - хабарламалар
```

#### API Эндпоинттері

**Ашық эндпоинттер:**
```bash
POST /api/auth/register     # Тіркелу
POST /api/auth/login        # Кіру
POST /api/auth/verify       # Токенді тексеру
```

**Қорғалған эндпоинттер (JWT қажет):**
```bash
GET  /api/auth/profile           # Профильді көру
PUT  /api/auth/profile           # Профильді жаңарту
PUT  /api/auth/change-password   # Құпия сөзді өзгерту
PUT  /api/auth/privacy-settings  # Құпиялық баптаулары
```

**Әкімші эндпоинттері (admin рөлі қажет):**
```bash
GET    /api/auth/admin/users              # Пайдаланушылар тізімі
PUT    /api/auth/admin/users/:id/role     # Рөлді өзгерту
PUT    /api/auth/admin/users/:id/status   # Статусты өзгерту (ban/unban)
DELETE /api/auth/admin/users/:id          # Пайдаланушыны жою
GET    /api/auth/admin/stats              # Жүйе статистикасы
GET    /api/auth/admin/logs               # Әкімші журналы
```

#### Әдепкі Әкімші Аккаунты

```
Username: admin
Email:    admin@travelmap.kz
Password: admin123
Role:     admin
```

⚠️ **МАҢЫЗДЫ:** Өндіріске шығарғанда құпия сөзді өзгертіңіз!

#### Пайдаланушы Рөлдері

1. **user** - қарапайым пайдаланушы
   - Профильді көру/өңдеу
   - Орындарды сақтау және пікір жазу
   - Бару орындары тарихын қарау

2. **moderator** - модератор
   - User рөлінің барлық мүмкіндіктері
   - Пікірлерді модерациялау
   - Контентті басқару

3. **admin** - әкімші
   - Барлық мүмкіндіктер
   - Пайдаланушыларды басқару
   - Рөлдерді өзгерту
   - Пайдаланушыларды бұғаттау/жою
   - Жүйе статистикасын көру
   - Әкімші журналын қарау

#### Frontend Беттер

1. **login.html** - Кіру/Тіркелу беті
   - Екі форма (кіру және тіркелу)
   - Құпия сөз күшін тексеру
   - Автоматты редирект (admin немесе profile)

2. **profile.html** - Пайдаланушы профилі
   - Жеке деректерді өңдеу
   - Құпия сөзді өзгерту
   - Құпиялық баптаулары
   - Статистика (бару орындары, пікірлер, т.б.)

3. **admin.html** - Әкімші панелі
   - Пайдаланушылар тізімі
   - Іздеу және сүзгілер
   - Рөлдерді өзгерту
   - Бұғаттау/разбан
   - Пайдаланушыларды жою
   - Жүйе статистикасы
   - Әкімші журналы

#### Қауіпсіздік Ерекшеліктері

- ✅ JWT токендер (7 күнге)
- ✅ bcrypt хэштеу (10 rounds)
- ✅ Құпия сөз валидациясы (минимум 6 таңба)
- ✅ Email формат тексеру
- ✅ Username уникалдығы
- ✅ SQL injection қорғанысы (parameterized queries)
- ✅ Role-based access control (RBAC)
- ✅ Өзін өзі жою қорғанысы (админ)
- ✅ Әкімші әрекеттері журналы

#### Тестілеу

```bash
# Тіркелу
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","email":"test@test.kz","password":"test123"}'

# Кіру
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# Профильді көру (TOKEN - жоғарыдан алынған)
curl http://localhost:3000/api/auth/profile \
  -H "Authorization: Bearer TOKEN"
```

---

## 🌐 ТОЛЫҚ ЖҮЙЕ АРХИТЕКТУРАСЫ

```
┌─────────────────────────────────────────────────────────┐
│                  FRONTEND (HTML/JS/CSS)                  │
│  index.html | login.html | profile.html | admin.html    │
│  monitor.html | API Client | LocalStorage (JWT)         │
└─────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────┐
│                    API GATEWAY (:3000)                   │
│  CORS | Body Parser | Health Check | Proxy Routes       │
└─────────────────────────────────────────────────────────┘
         │           │           │           │          │
         ▼           ▼           ▼           ▼          ▼
   ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
   │ Places  │ │ Weather │ │   Map   │ │  Chat   │ │  Auth   │
   │  :3001  │ │  :3002  │ │  :3003  │ │  :3004  │ │  :3005  │
   └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘
        │                                                  │
        ▼                                                  ▼
   ┌─────────┐                                      ┌──────────┐
   │ places  │                                      │ SQLite3  │
   │  .json  │                                      │    DB    │
   └─────────┘                                      └──────────┘
```

